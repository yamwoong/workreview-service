# 🚀 TypeScript 풀스택 프로젝트 기술 스택 가이드

> **Tech Stack**: TypeScript + Express + MongoDB + React + Node.js

---

## 📦 백엔드 (Node.js + Express + TypeScript)

### 🔒 보안 & 인증
| 라이브러리 | 용도 | 우선순위 |
|-----------|------|---------|
| `helmet` | 보안 HTTP 헤더 설정 | ⭐⭐⭐ |
| `cors` | Cross-Origin 요청 처리 | ⭐⭐⭐ |
| `express-rate-limit` | API 속도 제한 (DDoS 방지) | ⭐⭐⭐ |
| `jsonwebtoken` | JWT 토큰 인증 | ⭐⭐⭐ |
| `bcrypt` | 비밀번호 해싱 | ⭐⭐⭐ |

### ✅ 데이터 검증
| 라이브러리 | 용도 | 추천도 |
|-----------|------|--------|
| `zod` | TypeScript 친화적 스키마 검증 | ⭐⭐⭐ (추천) |
| `joi` | 대안적인 검증 라이브러리 | ⭐⭐ |

### 🗄️ 데이터베이스
| 라이브러리 | 용도 | 특징 |
|-----------|------|------|
| `mongoose` | MongoDB ODM | 전통적인 선택 |
| `Prisma` | 차세대 ORM | 타입 안전성 최고 ⭐ |

### 📝 로깅 & 모니터링
| 라이브러리 | 용도 | 추천도 |
|-----------|------|--------|
| `winston` | 프로덕션 로깅 | ⭐⭐⭐ |
| `pino` | 고성능 로깅 | ⭐⭐⭐ (더 빠름) |
| `morgan` | HTTP 요청 로깅 | ⭐⭐ |

### 🛠️ 개발 편의성
```bash
nodemon              # 파일 변경 시 자동 재시작
ts-node-dev          # TypeScript 개발 서버
dotenv               # 환경변수 관리
express-async-errors # async/await 에러 핸들링 자동화
```

### 🌐 국제화 (Internationalization)
| 라이브러리 | 용도 | 추천도 |
|-----------|------|--------|
| `i18next` | 다국어 지원 프레임워크 | ⭐⭐⭐ |
| `i18next-fs-backend` | 파일 시스템 기반 번역 로드 | ⭐⭐⭐ |
| `i18next-http-middleware` | Express 미들웨어 (언어 감지) | ⭐⭐⭐ |

**설정 예시:**
```typescript
// backend/src/config/i18n.ts
import i18next from 'i18next';
import Backend from 'i18next-fs-backend';
import middleware from 'i18next-http-middleware';

i18next
  .use(Backend)
  .use(middleware.LanguageDetector)
  .init({
    fallbackLng: 'en',
    preload: ['en', 'ko'],
    backend: {
      loadPath: './locales/{{lng}}/{{ns}}.json',
    },
  });
```

---

## 🎨 프론트엔드 (React + TypeScript)

### 🔄 상태 관리
| 라이브러리 | 추천 용도 | 특징 |
|-----------|----------|------|
| `zustand` | 소~중규모 앱 | 가볍고 간단 ⭐ |
| `Redux Toolkit` | 대규모 앱 | 강력하지만 복잡 |
| `TanStack Query` | 서버 상태 관리 | **필수!** ⭐⭐⭐ |

### 💅 UI & 스타일링
| 라이브러리 | 용도 | 추천도 |
|-----------|------|--------|
| `Tailwind CSS` | 유틸리티 CSS 프레임워크 | ⭐⭐⭐ |
| `shadcn/ui` | 커스터마이징 가능한 컴포넌트 | ⭐⭐⭐ (최신 트렌드) |
| `Material-UI` | 완성도 높은 컴포넌트 라이브러리 | ⭐⭐ |
| `Ant Design` | 엔터프라이즈급 UI | ⭐⭐ |
| `Framer Motion` | 애니메이션 | ⭐⭐ |

### 📋 폼 관리
```typescript
react-hook-form  // 성능 최적화된 폼 관리
zod              // 타입 안전한 폼 검증 (백엔드와 공유 가능!)
```

### 🧭 라우팅
```bash
react-router-dom@6  # 클라이언트 사이드 라우팅 필수
```

### 🌐 국제화 (Internationalization)
| 라이브러리 | 용도 | 추천도 |
|-----------|------|--------|
| `i18next` | 다국어 지원 프레임워크 | ⭐⭐⭐ |
| `react-i18next` | React 통합 | ⭐⭐⭐ |
| `i18next-browser-languagedetector` | 브라우저 언어 자동 감지 | ⭐⭐⭐ |

**설정 예시:**
```typescript
// frontend/src/i18n/config.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { translation: enTranslations },
      ko: { translation: koTranslations },
    },
    fallbackLng: 'en',
    detection: {
      order: ['localStorage', 'navigator'],
      caches: ['localStorage'],
    },
  });
```

**사용 예시:**
```typescript
// 컴포넌트에서 사용
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t, i18n } = useTranslation();

  return (
    <div>
      <h1>{t('welcome.title')}</h1>
      <button onClick={() => i18n.changeLanguage('ko')}>
        한국어
      </button>
    </div>
  );
}
```

---

## 🐳 DevOps & 배포

### 컨테이너화 (필수!)
```dockerfile
Docker               # 컨테이너 생성
Docker Compose       # 멀티 컨테이너 오케스트레이션
```

### 🔄 CI/CD
- **GitHub Actions** (무료, 간편)
- **GitLab CI**
- **Jenkins** (대규모)

### ☁️ 배포 플랫폼

#### 프론트엔드
| 플랫폼 | 특징 | 가격 |
|--------|------|------|
| **Vercel** | Next.js 최적화, 자동 배포 | Free tier 좋음 ⭐ |
| **Netlify** | 간편한 설정 | Free tier 좋음 |
| **Cloudflare Pages** | 빠른 CDN | Free tier 매우 좋음 |

#### 백엔드
| 플랫폼 | 특징 | 가격 |
|--------|------|------|
| **Railway** | 간편한 배포, DB 포함 | $5/월부터 ⭐ |
| **Render** | 무료 시작 가능 | Free tier 있음 |
| **AWS EC2** | 완전한 제어 | 유료 (저렴 가능) |
| **DigitalOcean** | 간단한 VPS | $4/월부터 |

#### 데이터베이스
```
MongoDB Atlas  # 관리형 MongoDB (무료 tier 500MB) ⭐⭐⭐
```

---

## 🧪 테스팅

### 백엔드 테스트
```bash
Jest          # 테스트 프레임워크
supertest     # HTTP 요청 테스트
@faker-js/faker  # 테스트 데이터 생성
```

### 프론트엔드 테스트
```bash
Vitest                    # Jest 대체 (Vite 친화적)
@testing-library/react    # React 컴포넌트 테스트
@testing-library/user-event  # 사용자 이벤트 시뮬레이션
```

### E2E 테스트
| 도구 | 특징 | 추천도 |
|------|------|--------|
| **Playwright** | 빠르고 현대적 | ⭐⭐⭐ (추천) |
| **Cypress** | 개발자 경험 좋음 | ⭐⭐ |

---

## ✨ 코드 품질

### Linting & Formatting
```json
{
  "devDependencies": {
    "eslint": "^8.x",
    "@typescript-eslint/parser": "^6.x",
    "@typescript-eslint/eslint-plugin": "^6.x",
    "prettier": "^3.x",
    "eslint-config-prettier": "^9.x"
  }
}
```

### Git Hooks
```bash
husky          # Git 훅 관리
lint-staged    # Staged 파일만 린트
```

**설정 예시:**
```json
{
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
```

---

## 📊 모니터링 & 성능

### 에러 트래킹
```
Sentry  # 실시간 에러 모니터링 및 알림 ⭐⭐⭐
```

### 프로세스 관리
```bash
PM2  # Node.js 프로세스 관리 (프로덕션 필수)
     # - 자동 재시작
     # - 로드 밸런싱
     # - 로그 관리
```

### 성능 최적화
| 라이브러리/서비스 | 용도 | 효과 |
|------------------|------|------|
| `compression` | gzip 압축 | 응답 크기 70% 감소 |
| `Redis` | 캐싱 & 세션 | 속도 10-100배 향상 |
| **CDN** (Cloudflare) | 정적 파일 배포 | 글로벌 속도 향상 |

---

## 📚 문서화

```bash
swagger-jsdoc       # JSDoc으로 API 문서 자동 생성
swagger-ui-express  # Swagger UI 제공

# 접근: http://localhost:3000/api-docs
```

---

## 🎯 프로젝트별 추천 조합

### 🏃‍♂️ 빠른 시작 (MVP)
```
Frontend: React + Vite + Tailwind + zustand + i18next
Backend: Express + TypeScript + Mongoose + JWT
Deploy: Vercel (FE) + Railway (BE) + MongoDB Atlas
```

### 🏢 엔터프라이즈급
```
Frontend: React + TypeScript + Redux Toolkit + TanStack Query + Material-UI + i18next
Backend: Express + TypeScript + Prisma + Redis + Winston + i18next
Testing: Jest + Playwright + Sentry
Deploy: AWS (ECS) + CloudFront + RDS
CI/CD: GitHub Actions
```

### 💡 현대적 스택 (추천)
```
Frontend: React + Vite + Tailwind + zustand + TanStack Query + shadcn/ui + i18next
Backend: Express + TypeScript + Prisma + Zod + Winston + i18next
Testing: Vitest + Playwright
Deploy: Vercel + Railway + MongoDB Atlas
Tools: Docker + GitHub Actions + Sentry
i18n: Full internationalization support (English, Korean, etc.)
```

---

## 🚀 시작하기

### 1. 프로젝트 초기화
```bash
# 백엔드
mkdir backend && cd backend
npm init -y
npm install express mongoose cors helmet dotenv
npm install -D typescript @types/node @types/express ts-node-dev

# 프론트엔드
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install react-router-dom @tanstack/react-query zustand
npm install -D tailwindcss postcss autoprefixer
```

### 2. TypeScript 설정
```bash
npx tsc --init
# strict: true로 설정!
```

### 3. Docker 설정
```dockerfile
# backend/Dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

---

## 📌 체크리스트

배포 전 확인사항:

- [ ] 환경변수 설정 (`.env.production`)
- [ ] CORS 설정 확인
- [ ] Rate limiting 활성화
- [ ] 에러 핸들링 구현
- [ ] 로깅 설정
- [ ] 데이터베이스 인덱스 최적화
- [ ] Docker 이미지 빌드 테스트
- [ ] CI/CD 파이프라인 구축
- [ ] 모니터링 도구 연동 (Sentry)
- [ ] SSL/HTTPS 설정
- [ ] 백업 전략 수립

---

**작성일**: 2025년 11월  
**업데이트**: 프로젝트 시작 전 최신 버전 확인 필수! 📦
